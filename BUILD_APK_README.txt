PURNATA ENTERPRISE - APK BUILD

This project includes a GitHub Actions workflow at:
.github/workflows/build-apk.yml

When uploaded to a GitHub repository, GitHub Actions can build the Android release APK automatically.
The resulting file will be named app-release.apk inside the Purnata-Enterprise-APK artifact.

Core app data is stored locally on the phone using shared_preferences.
